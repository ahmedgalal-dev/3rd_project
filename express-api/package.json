{
  "name": "express-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npx tsc && cp package.json ./build/ && mkdir ./build/.elasticbeanstalk/ && cp -R .elasticbeanstalk ./build/",
    "start": "node ./server.js",
    "deploy":"cd build && eb deploy",
    "run-dev": "ts-node-dev --respawn -- src/index.ts",
    "run": "npm run build && node ./build/index.js",
    "create-db": "db-migrate db:create deployment_project_db",
    "create-test-db": "db-migrate db:create deployment_project_db_test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.13",
    "@types/jasmine": "^4.0.3",
    "@types/pg": "^8.6.5",
    "@types/supertest": "^2.0.12",
    "jasmine": "^4.1.0",
    "jasmine-spec-reporter": "^7.0.0",
    "supertest": "^6.2.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "@types/cors": "^2.8.12",
    "cors": "^2.8.5",
    "db-migrate": "^0.11.13",
    "db-migrate-pg": "^1.2.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "pg": "^8.7.3"
  }
}
